name: Daily Pipeline

on:
  schedule:
    - cron: '0 5 * * *'
  workflow_dispatch:

jobs:
  run_pipeline:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install requests beautifulsoup4 gdeltdoc networkx scikit-learn pandas

      - name: Step 1 — Fetch GDELT English media signals
        run: python pipeline/01_fetch_media.py

      - name: Step 2 — Fetch Chinese RSS signals
        run: python pipeline/02_fetch_chinese_rss.py

      - name: Step 3 — Score all layers
        run: python pipeline/03_score_layers.py

      - name: Step 4 — Combine scores and write scores.json
        run: python pipeline/04_combine_scores.py

      - name: Commit outputs
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data/media_history.json data/chinese_rss_history.json scores.json
          git diff --staged --quiet || git commit -m "chore: daily pipeline $(date -u +%Y-%m-%d)"
          git pull --rebase origin main
          git push
